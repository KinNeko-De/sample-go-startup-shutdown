## Overview

The `context` command serves as a minimal example showing:

- Starting application
- Waiting for signal
- Stopping a goroutine
- Shutdown application

## Usage

```bash
go run ./cmd/context
```